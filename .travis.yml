dist: trusty
sudo: required
language: node_js

node_js:
  - "7"

services:
  - docker

before_install:
  - ant -version
  - mvn -version
  - pwd
  - ls -lh
  - sudo npm install -g grunt-cli
  - ./.build_travis.sh

script:
  - docker build -f docker-sarit-app/Dockerfile -t sarit-webapp .
